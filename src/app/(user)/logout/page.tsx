import { Metadata } from "next";
import Account from "./Logout";
import Logout from "./Logout";

export const metadata: Metadata = {
    title: 'Logout',
    description: 'Logout',
}

export default Logout