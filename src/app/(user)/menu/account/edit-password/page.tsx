import { Metadata } from "next";
import EditPassword from "./EditPassword";

export const metadata: Metadata = {
    title: 'Account Edit Password',
    description: 'Account Edit Password',
}

export default EditPassword