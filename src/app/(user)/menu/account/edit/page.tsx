import { Metadata } from "next";
import Account from "./AccountEdit";
import AccountEdit from "./AccountEdit";

export const metadata: Metadata = {
    title: 'Account Edit',
    description: 'Account Edit',
}

export default AccountEdit